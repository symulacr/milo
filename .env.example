# Browser-safe values, served only through /api/public-config.
PRIVY_APP_ID=
CONVEX_URL=
MIDNIGHT_NETWORK=preprod

# Server credentials belong in the Convex deployment environment, NOT browser code.
# Use rotated test credentials; never store wallet seeds or private keys here.
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
